SUM: PROC ( X1, Y1 ) RETURNS (COMPLEX);
	DCL ( X1, Y1 ) FIXED;
	DCL R FIXED;
	R = X1 + Y1;
END SUM;

Main: PROC;
	DCL F FLOAT;
	DCL C COMPLEX;
	DCL D FIXED;
	DCL SUM FIXED;

	SUM = D;

END Main;
